
<div class="row">
<div class="col-6">
  <strong>Sku:</strong>
  <%= @variant.sku %>
</div>

<div class="col-6">
  <strong>Title:</strong>
  <%= @variant.title %>
</div>

<div class="col-6">
  <strong>Desc:</strong>
  <%= @variant.desc %>
</div>

<div class="col-6">
  <strong>Product:</strong>
  <%= @variant.product_id %>
</div>

</div>

<div class="row">
<div class="col-3 btn-group mt-5">
<%= link_to 'Edit', edit_variant_path(@variant) , class: 'btn btn-success' %> |
<%= link_to 'Back', variants_path, class: 'btn btn-outline-primary' %>
</div>
</div>
