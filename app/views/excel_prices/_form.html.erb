
<%= simple_form_for(@excel_price) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="row">
    <div class="col-12 h3"><%= page_title %></div>
    <div class="col-6 row">
    <%= f.input :title, label: 'Название'%>
    <%= f.input :link, label: 'Ссылка'%>
    <%= f.input :price_move, label: 'Действие', collection: [['+','plus'], ['-','minus']], include_blank: false, input_html: { class: 'form-select' }, wrapper_html: { class: 'col-auto' } %>
    <%= f.input :price_shift, label: 'Число', type: "number", wrapper_html: { class: 'col-auto' } %>
    <%= f.input :price_points, label: 'Тип',  collection: [['%','percents'], ['ед.','fixed']], include_blank: false, input_html: { class: 'form-select' }, wrapper_html: { class: 'col-auto' } %>
    <%= f.input :comment, label: 'Комментарий' %>    
    </div>
</div>
  <div class="row">
  <div class="col-3 btn-group mt-5">
    <%= f.button :submit, class: 'btn btn-success' %>
    <%= link_to 'Назад', :back, class: 'btn btn-outline-primary' %>
  </div>
  </div>
<% end %>
